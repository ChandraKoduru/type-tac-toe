name: type-tac-toe
version: 0.0.1.0
ghc-options: -Wall -Werror -Wno-unused-matches -Wincomplete-uni-patterns -Wincomplete-record-updates -Wmissing-import-lists

dependencies: 
  - base

executables:
  type-tac-toe:
    source-dirs: src
    main: TypeTacToe.hs 
    ghc-options: -main-is TypeTacToe

